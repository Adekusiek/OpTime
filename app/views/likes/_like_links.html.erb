<% if current_user.likes.find_by(event_id: event.id) %>
  <%= link_to 'いいね！を取り消す', unlike_path(event.id), method: :delete, remote: true %>
<% else %>
  <%= link_to 'いいね！', like_path(event.id), method: :post, remote: true %>
<% end %>
